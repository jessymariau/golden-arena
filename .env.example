# Get a key at https://openrouter.ai/keys
# Without this set, Golden Arena runs in DEMO MODE with mock model responses
# so you can see the whole thing work before spending anything.
OPENROUTER_API_KEY=
PORT=3000
